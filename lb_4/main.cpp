#include "task_runner.hpp"
#include "files_experiments.hpp"

int main() {
  // Создание пустых входных файлов, если их нет
  // prepare_input_files();

  // run_task(1, 1);
  // run_task(1, 2);
  // run_task(1, 3);
  // run_task(2, 1);
  // run_task(2, 2);
  // run_task(2, 5);
  // run_task(3, 1);
  // run_task(3, 5);
  // run_task(4, 5);
  // run_task(5, 1);
  run_task(6, 1);
  run_task(7, 1);
}